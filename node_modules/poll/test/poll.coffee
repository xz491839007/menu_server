# todo write proper tests

poll = require("./poll")

o = "hey"

p = poll(->o).every(200).for(2000).now()
p.progress((->console.log("progress", arguments)))
p.then(-> console.log('done'))

setTimeout((->o = "heysann"), 300)